[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "quant-open-source-lab"
version = "0.1.0"
description = "Reproducible experiments comparing open-source quant engines."
readme = "README.md"
requires-python = ">=3.11,<3.12"
license = { file = "LICENSE" }
dependencies = [
  "backtrader==1.9.78.123",
  "numpy>=1.26,<3",
  "pandas>=2.2,<3",
]

[project.scripts]
quant-lab = "quant_open_source_lab.cli:main"

[dependency-groups]
dev = ["pytest>=8,<9", "ruff>=0.12,<1"]

[tool.hatch.build.targets.wheel]
packages = ["src/quant_open_source_lab"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
